{
  "name": "demo-inmobiliaria",
  "version": "1.0.0",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "start": "yts --import=./env.mjs --watch --watch-path=src --watch-preserve-output ./src/run.ts",
    "fmt": "prettier --write .",
    "tsc": "tsc --noEmit",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@wabot-dev/framework": "^0.1.0-beta.1",
    "qrcode-terminal": "^0.12.0",
    "whatsapp-web.js": "1.31.0"
  },
  "devDependencies": {
    "@types/qrcode-terminal": "^0.12.2",
    "prettier": "^3.5.3"
  }
}
